@model IEnumerable<LamborghiniAuto.Models.Preventivo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table text-white">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nomeCl)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.cognomeCl)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.numeroCl)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nomeCl)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.cognomeCl)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.numeroCl)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
